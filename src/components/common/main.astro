---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'main'> {}

const { class: className, ...props } = Astro.props
---

<main
  class:list={['mx-auto mb-48 max-w-screen-md px-4 md:px-0', className]}
  {...props}
>
  <slot />
</main>
